const Home = (props) => {
  // const [user, setUser] = props.fields;

  return (
    <section>
      {/* <h4 style={{color: "#4895ef"}}>Hi, {user.username}!</h4> */}
    </section>
  );
};

export default Home;